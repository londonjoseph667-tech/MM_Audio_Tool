<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme — used by the Activity window before Compose takes over -->
    <style name="Theme.MM_Audio_Tool" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorSecondary">@color/secondary_teal</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
